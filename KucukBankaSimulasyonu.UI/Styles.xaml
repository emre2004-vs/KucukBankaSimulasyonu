<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:KucukBankaSimulasyonu.UI.Converters">

    <!-- 🔹 MENU BUTTON -->
    <Style x:Key="MenuButton" TargetType="Button">
        <Setter Property="Foreground" Value="#E5E7EB"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- 🔹 ACTION BUTTONS -->
    <Style x:Key="ActionButtonBlue" TargetType="Button">
        <Setter Property="Background" Value="#2563EB"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style x:Key="ActionButtonGreen" TargetType="Button">
        <Setter Property="Background" Value="#22C55E"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style x:Key="ActionButtonTeal" TargetType="Button">
        <Setter Property="Background" Value="#14B8A6"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- 🔹 INFO CARD -->
    <Style x:Key="InfoCard" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="CornerRadius" Value="14"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="15"
                                  ShadowDepth="2"
                                  Opacity="0.15"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 🔹 ISLEM RENK CONVERTER (HATA BURADA ÇÖZÜLDÜ) -->
    <converters:IslemRenkConverter x:Key="IslemRenkConverter"/>

</ResourceDictionary>
